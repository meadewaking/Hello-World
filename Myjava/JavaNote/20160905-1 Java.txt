4. 构造方法特性：
（1）命名：与类名相同：
（2）返回值：无返回值，不是返回void；
（3）有参数，经常被重载；
（4）不能直接调用，必须同new关键字调用，new完成了申请内存并返回对象；
（5）通常是public的，private的依然有意义；
（6）类中未定构造方法，系统会提供默认无参数构造方法，

  重载是在同一区域内（在一个类内），同名但参数不同（个数、类型、顺序）的函数互为重载，与形参名无关，与返回值类型无关。
  同名函数执行结果不同，称之为多态，重载是静态的多态，编译前即可确定调用哪个重载。